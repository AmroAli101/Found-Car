<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Car Check</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">
</head>

<body>
  <div class="container mt-5">
    <h1 class="text-center">Check Your Car Status 🚗</h1>

    <!-- Form to input car number -->
    <form action="/" method="POST" class="my-4">
      <div class="form-group">
        <label for="carNumber">Enter Car Number:</label>
        <input type="text" name="carNumber" id="carNumber" class="form-control" placeholder="Enter car number" required>
      </div>
      <button type="submit" class="btn btn-primary btn-block">Check</button>
    </form>

    <!-- Section to display car information -->
    <div class="mt-4">
      <% if (data) { %>
        <div class="card">
          <div class="card-body">
            <h5 class="card-title">Manufacturer: <%= data.tozeret_nm %></h5>
            <p>Model: <%= data.degem_nm %></p>
            <p>Color: <%= data.tzeva_rechev %></p>
            <p>Owner: <%= data.baalut %></p>
            <p>Car Number: <%= data.mispar_rechev %></p>
          </div>
        </div>
      <% } %>

      <% if (error) { %>
        <div class="alert alert-danger mt-3">
          <%= error %>
        </div>
      <% } %>
    </div>
  </div>

  <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/popper.js@1.12.9/dist/umd/popper.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/js/bootstrap.min.js"></script>
</body>

</html>
